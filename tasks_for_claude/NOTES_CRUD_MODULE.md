1) Add new module 'notes' to project
2) notes module should implement CRUD operations for notes
3) Notes should have id, text (mandatory field), date of creation(set automatically), due date(optional), priority (
   optional), completion sign (boolean, false by default)
4) notes module should receive CRUD rest requests
5) notes Module should store notes in local postgres database
6) Database must have structure for storing notes_