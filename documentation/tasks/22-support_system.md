Модифицируй приложение так, чтобы оно стало ИИ-агентом поддержки пользователей.
Нужен MCP, который будет подключаться к БД postgresql и работать с несложными тикетами поддержки (id,
название,инициатор, описание, приоритет)
MCP должен предоставлять стандартные crud-операции + несколько операций выборки и фильтрации (по инициатору, по названию
и тд)
Модифицируй system prompt так, чтобы LLM openrouter выступал в виде специалиста поддержики по текущему приложению
и старался отвечать опираясь на данные из RAG (туда загружена кодовая база текущего приложения) и учитывал существующие
тикеты поддержки