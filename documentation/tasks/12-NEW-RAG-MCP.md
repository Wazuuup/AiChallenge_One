add new module "rag" to "mcp" folder
use @builder-ktor-backend agent.
use kotlin, ktor, koin and exposed.
Use kotlin-sdk to generate mcp server.
This module have to provide mcp tools for /api/rag/search endpoint of services/rag module
