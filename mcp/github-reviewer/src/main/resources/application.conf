ktor {
  deployment {
    # HTTP port
    port = 8095
    port = ${?PORT}

    # HTTPS/SSL port
    ssl_port = 8451
    ssl_port = ${?SSL_PORT}
  }
  application {
    modules = [ru.sber.cb.aichallenge_one.mcp_github_reviewer.ApplicationKt.module]
  }
}

# GitHub configuration
github {
  # GitHub Personal Access Token (required)
  token = ${?GITHUB_TOKEN}
}
